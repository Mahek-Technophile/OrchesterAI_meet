{
  "name": "google-meet-system",
  "version": "1.0.0",
  "description": "A system to generate Google Meet links and share them via email, WhatsApp, and calendar",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start",
    "setup": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "keywords": ["google-meet", "gmail", "calendar", "whatsapp", "oauth2"],
  "author": "",
  "license": "MIT"
}